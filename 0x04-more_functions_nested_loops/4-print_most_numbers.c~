#include "main.h"
#include <stdio.h>

/**
 * print_most_numbers - prints numbers 0-9 except 2 & 4
 * @n: number holder
 * Return: always 0
 */
void print_most_numbers(void)
{
	int n;

	while (n <= 9)
	{
		if (n != 2 && n !=4)
			_putchar(n);
	}
	_putchar("\n");
	return (0);
}
